# Блокеры и известные ограничения

Снимок: 23 сентября 2026 года. Проверенный commit кода — `8a9a6198417a9893ea8d360d96513418e31de319`. Область — интеграция ветки `codex/ekt-site-v03`, локальное QA и подготовка раннего размещения. Подробные доказательства и матрица пяти проверок: [final-qa.md](final-qa.md).

Основной локальный синтетический сценарий работает; блокер сессии между вкладками исправлен и повторно проверен. **Готовность публичного стенда и полная приёмка по данным ekt.kz не подтверждены.** Открытые внешние зависимости ниже отделены от воспроизводимых ошибок продукта. Финальный полный прогон: **159 passed за 20,28 с**, Ruff и `pip check` прошли.

## Открытые блокеры внешнего стенда и данных

| ID | Статус / область | Проблема и влияние | Владелец | Условие закрытия |
|---|---|---|---|---|
| Q-DEPLOY-01 | **BLOCKED**, внешняя демонстрация | Нет внешнего URL и выбранного доступного размещения. Локальный `127.0.0.1:5173` доступен только на компьютере запуска. Входной контроль и cookie ещё не проверены через настоящий HTTPS-прокси. | Участник 3 + владелец хостинга/доступа | Получен HTTPS URL; запуск с одним worker и `PUBLIC_DEMO=true`; вход, чат, подтверждение, `/cart` и обновление старой вкладки успешно проверены с внешнего браузера. Настроен доверенный прокси, чтобы внешний Origin и схема запроса совпадали. |
| Q-DEPLOY-02 | **BLOCKED / CI FAILED**, контейнерный выпуск | [Actions run 35863270635](https://github.com/BAITC-Hacks/hack-3c5e22be-team/actions/runs/35863270635) для проверенного commit завершился failure до запуска шагов: backend job 107188446312 и container job 107188446373 имеют `steps: []`; 2026-09-23 12:52:31–12:52:35 UTC. Логи обеих jobs возвращают `404 BlobNotFound`; точная причина недоступна через текущий коннектор и не установлена. Docker локально отсутствует, контейнер не проверен. | Владелец репозитория / сопровождающий GitHub Actions + Участник 3 | Владелец проверяет annotations запуска и настройки Actions, устраняет установленную причину и повторяет CI. Успешный прогон либо отдельная машина с Docker собирает образ, запускает контейнер и проверяет health + frontend + создание сессии и корзину; сохранены статус, commit и ссылка на лог. |
| Q-DATA-01 | **BLOCKED**, положительный сценарий сертификата | Нет настоящего сертификата партнёра с проверенной связью с артикулом. Синтетический каталог не содержит положительного примера документа. Полный пункт 1 приёмки не закрыт. | Партнёр EKT + Backend | Предоставлен документ/ссылка и связь с артикулом; запрос возвращает именно этот документ, ссылка открывается. Отдельно проверяется товар без сертификата. |
| Q-DATA-02 | **BLOCKED**, проверка аналогов на настоящих товарах | Синтетическая пара `DEMO-001` → `DEMO-002` проверена, но настоящая пара «нулевой остаток → доступный кандидат» с достаточными техническими параметрами не подтверждена. | Партнёр EKT + Backend | Выбраны реальные артикулы, получены свежие остатки и обязательные характеристики; объяснение совпадений и отличий сверено с источником, неподходящий товар исключён. |

## Открытый дефект вне блокеров интеграции

| ID | Приоритет / статус | Воспроизведение и фактический результат | Владелец | Условие закрытия |
|---|---|---|---|---|
| QA-INPUT-01 | **P2 / OPEN** | `GET /api/products/999999999999999999999999999999999999` в изолированном TestClient возвращает `500 Internal Server Error`. Ожидается контролируемый 4xx для ID вне поддерживаемого диапазона. Причина: целое число передаётся SQLite без проверки диапазона. Не препятствует основному демо, но оставляет отрицательную приёмку незавершённой. | Backend | Добавлена проверка допустимого диапазона ID до SQLite, проверены endpoint карточки и аналогов, добавлена регрессия; чрезмерно большой ID возвращает структурированный 4xx. |

## Исправленные блокеры

| ID | Статус | Что было исправлено | Подтверждение |
|---|---|---|---|
| Q-UI-01 | **FIXED** | При `PUBLIC_DEMO=true` минимальный `/health` скрывал `cart_enabled` и `catalog_count` даже после входа: интерфейс отключал корзину и показывал неизвестное число товаров. Теперь без входа доступен только статус, после валидных Basic/cookie — исходная конфигурация, с `Cache-Control: no-store`. | `test_authenticated_health_preserves_frontend_configuration` проверяет оба способа входа; 32 gate-теста входят в финальный полный прогон 159 passed. |
| QA-SESSION-02 | **P1 / FIXED** | Новая вкладка создавала новую сессию и заменяла cookie корзины. Исправлено: cookie имеет `Path=/`, прежняя cookie `Path=/cart` удаляется; после проверки Origin действительная сессия повторно используется без продления TTL. Явный reset завершает сессию и удаляет cookie обоих путей. | `test_second_tab_resumes_cart_until_explicit_reset`, `test_expired_browser_cookie_creates_new_session` входят в финальный полный прогон. После перезапуска браузер повторён: подтверждены 2 единицы во вкладке А; вкладка Б `/` и старая `/cart` после обновления показывают те же 2 единицы, 2400 KZT, версию 1. Владелец исправления — Участник 3 / Backend. |
| Q-INTEGRATION-01 | **FIXED локально** | Frontend и backend обслуживаются одним FastAPI-приложением; API-клиент использует относительные пути. Ссылка `/cart` относится к тому же origin и текущей cookie-сессии. | Четыре теста `test_site.py` прошли; в браузере подтверждено добавление, изменение 2 → 3 и актуальные 3600 KZT в ранее открытой вкладке `/cart` после обновления; отдельно после исправления сессии проверены две вкладки. Внешний повтор относится к Q-DEPLOY-01. |

## Ограничения текущего прототипа

| ID | Ограничение | Как показывать на демонстрации / дальнейшее условие |
|---|---|---|
| LIMIT-CART-01 | Корзина ekt.kz и оформление настоящего заказа не подключены. Корзина, сессии и подтверждения принадлежат прототипу. | Явно обозначать это в интерфейсе и питче. Для реальной интеграции партнёр должен предоставить контракт и доступ к API корзины/заказа, после чего потребуется новая приёмка. |
| LIMIT-DATA-01 | Демонстрационные артикулы, цены и остатки синтетические. Тесты с именем `live` используют подставленный клиент источника. | Не выдавать за доказательство наличия товара у EKT или выполненных запросов к рабочему API. |
| LIMIT-BUDGET-01 | Общий лимит запросов и gate-cookie рассчитаны на один процесс. Лимиты и ключ подписи сбрасываются при перезапуске; сессии корзины также не переживают рестарт. | Запускать один worker; не обещать постоянную историю/резервирование и денежный лимит OpenAI на основании счётчика запросов. |
| LIMIT-TERMS-01 | Общий минимум заказа в источнике не указан; опубликованные пороги бесплатной доставки противоречат друг другу. | Показывать источник и необходимость уточнения; не превращать неизвестные значения в обещание клиенту. |

## Незавершённые проверки текущего снимка

- Мобильный браузерный сценарий не подтверждён: запрос ширины 390 не изменил снимок 1280 × 720. Ранее заявленные исходной веткой пять размеров экрана не заменяют новый прогон. Ответ об условиях со ссылкой, датой и неизвестным минимумом проверен в текущем браузере.
- Видеофайл не записан. Имеются текстовый сценарий, структура питча из пяти слайдов и скриншоты в контексте чата; это подготовка к записи, а не готовое видео. Владелец — Участник 3; закрытие — записанный и просмотренный файл либо доступная ссылка на запись.

Этот реестр отделяет реальные найденные ошибки от отсутствующего доступа, данных и ограничений согласованного демонстрационного режима. Закрывать пункт следует по указанному доказательству, с привязкой к проверенному commit.
